# ES管理服务完整依赖列表 - 用于离线安装

# 核心Web框架
flask==2.3.3
flask-cors==4.0.0

# 环境配置
python-dotenv==1.0.0

# Elasticsearch客户端
elasticsearch==7.17.0

# 任务调度
schedule==1.2.0

# 达梦数据库驱动（需要单独下载dmPython安装包）
# dmPython  # 需要从达梦官网下载对应版本

# 以下是间接依赖，建议一起下载
certifi>=2017.4.17
charset-normalizer>=2.0.0
click>=8.0.0
idna>=2.5
itsdangerous>=2.0.0
Jinja2>=3.0.0
MarkupSafe>=2.0.0
requests>=2.25.0
urllib3>=1.26.0
Werkzeug>=2.3.0

# 并发处理（Python标准库，无需额外安装）
# threading
# concurrent.futures
# queue

# 系统工具（Python标准库，无需额外安装）
# os
# json
# time
# datetime
# ssl